# macOS
.DS_Store

node_modules/
dist/
build/

# noVNC + pako — copy in with scripts/vendor-novnc.sh or frontend ensure-novnc (see docs/spec/noVNC.md)
frontend/public/vendor/
.env
.pids/
.logs/
*.log
reports/
# Security-audit drafts kept private until triaged (may list unpatched findings).
# Fully-addressed reviews live tracked under docs/review/ alongside this.
# Reversible: `git add -f docs/review/2026-07-05/...` to track a redacted copy.
docs/review/2026-07-05/
config/wisp-config.json
config/runtime.env
config/wisp-password

# Claude Code working directory (personal settings + runtime artifacts).
# Carve out exceptions (e.g. !.claude/settings.json) if shared content
# is ever added.
.claude/
