[Unit]
Description=Wisp — Updater (atomic swap to a staged release)
After=network.target

[Service]
Type=oneshot
User=root
Environment=WISP_INSTALL_DIR=WISP_PATH
ExecStart=/usr/local/bin/wisp-updater
StandardOutput=journal
StandardError=journal
TimeoutStartSec=600
